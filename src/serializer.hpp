/**
 * Save the instructions to the file, which is called opcode.
 * And deserialize it to instructions. The instructions are defined at opcode.hpp.
 */


#define SERIALIZER_H

namespace CS {

class Serializer {

    public:    
        
    static void Serialize(vector<Instruction> is, const char* file_path) {

    }

    static vector<Instruction> Deserialize(const char* file_path) {

    }


    private:

};
}
